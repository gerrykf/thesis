# 登录流程中获取该用户权限

登录后应该返回该用户的角色 role_id,然后根据角色 role_id 获取该用户的权限列表 保存权限标识到 userSotore 的 permissions

流程：

1. 服务端在登录成功后 根据该用户的角色 role_id 查询该角色对应的权限列表 permissions
2. 将权限列表 permissions 加到 User 模型中 一起返回给前端
3. 前端在登录成功后 将权限列表 permissions 保存到 userStore 中
